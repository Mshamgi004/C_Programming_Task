// Create a C program using the pointers with the help of string to calculate the length of string

#include<stdio.h>
#include<string.h>
#include "pointerarith.h"

int main()
{
		
	
	printf("The length of first string = %d \n", stringLength("Test1"));
	printf("The length of second string = %d \n", stringLength(""));
	printf("The length of third string = %d \n", stringLength("Test 2"));
	
	return 0;
}


			
